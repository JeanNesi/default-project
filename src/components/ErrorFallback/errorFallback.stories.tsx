import { ErrorFallback } from "."; // Assuming your component is in './ErrorFallback'

export default {
  title: "Components/ErrorFallback",
  component: ErrorFallback,
};

export const Default = () => <ErrorFallback />;
